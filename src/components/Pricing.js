import React from 'react';
import './Pricing.css'; // Import the CSS file

const Pricing = () => {
  return (
    <section id="pricing" className="pricing-section">
      <h1>Pricing</h1>
      <p>Find the perfect pricing for you</p>
    </section>
  );
};

export default Pricing;
